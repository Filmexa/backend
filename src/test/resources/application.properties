spring.application.name=stream-test

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/filmexa_test}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

security.jwt.secret-key=d96ac79641c2dbc2a040e31ad167b7baa8157008c19d62fb9de0af75355adad7
security.jwt.expiration-time=3600000
security.jwt.refresh-expiration-time=604800000
security.verification.expiration-minutes=15

spring.mail.host=localhost
spring.mail.port=2525
spring.mail.username=test@filmexa.com
spring.mail.password=test-password

ft.client.id=test-ft-client-id
ft.client.secret=test-ft-client-secret
ft.redirect-uri=http://localhost:8080/api/auth/42/callback
ft.authorize-url=https://api.intra.42.fr/oauth/authorize
ft.token-url=https://api.intra.42.fr/oauth/token
ft.me-url=https://api.intra.42.fr/v2/me

google.client.id=test-google-client-id
google.client.secret=test-google-client-secret
google.redirect-uri=http://localhost:8080/api/auth/google/callback
google.authorize-url=https://accounts.google.com/o/oauth2/v2/auth
google.token-url=https://oauth2.googleapis.com/token
google.userinfo-url=https://www.googleapis.com/oauth2/v3/userinfo

facebook.client.id=test-facebook-client-id
facebook.client.secret=test-facebook-client-secret
facebook.redirect-uri=http://localhost:8080/api/auth/facebook/callback
facebook.authorize-url=https://www.facebook.com/v19.0/dialog/oauth
facebook.token-url=https://graph.facebook.com/v19.0/oauth/access_token
facebook.userinfo-url=https://graph.facebook.com/me

app.cors.allowed-origins=http://localhost:4200

app.rate-limit.enabled=false

app.img-storage-path=${java.io.tmpdir}/filmexa-test-images

spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

#TMDB
tmdb.access-token=${TMDB_ACCESS_TOKEN}
tmdb.base-url=${TMDB_BASE_URL}
tmdb.base-image-url=${TMDB_IMAGE_BASE_URL}

# YTS
yts.base-url=${YTS_BASE_URL:https://yts.mx/api/v2}

# PIRATBAY
piratebay.base-url=${PIRATEBAY_BASE_URL:https://piratebay.org}

# Streaming / transcoding
app.stream.segment-seconds=${STREAM_SEGMENT_SECONDS:6}
app.stream.ffmpeg-path=${FFMPEG_PATH:ffmpeg}
app.stream.ffprobe-path=${FFPROBE_PATH:ffprobe}
app.stream.preset=${STREAM_PRESET:veryfast}
app.stream.max-concurrent-transcodes=${STREAM_MAX_TRANSCODES:4}
app.stream.transcode-timeout-seconds=${STREAM_TRANSCODE_TIMEOUT:120}
app.stream.probe-timeout-seconds=${STREAM_PROBE_TIMEOUT:30}
app.stream.token-secret=${STREAM_TOKEN_SECRET:${SECURITY_JWT_SECRET_KEY}}
app.stream.token-ttl-minutes=${STREAM_TOKEN_TTL_MINUTES:180}
# How many seconds of video must already be on disk ahead of a requested segment
app.stream.readahead-seconds=${STREAM_READAHEAD_SECONDS:15}

# Cleanup: movies unwatched for this long are erased (42 subject: one month)
app.cleanup.retention-days=${CLEANUP_RETENTION_DAYS:30}
app.cleanup.cron=${CLEANUP_CRON:0 0 3 * * *}

app.magnet.fallback=${MAGNET_FALLBACK:big-buck-bunny}

spring.config.import=optional:file:.env[.properties]
